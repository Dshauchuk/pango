<abstract:PageBase
    x:Class="Pango.Desktop.Uwp.Views.PasswordsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Pango.Desktop.Uwp.Controls"
    xmlns:loc="using:Pango.Desktop.Uwp.Core.Localization"
    xmlns:viewmodels="using:Pango.Desktop.Uwp.ViewModels"
    xmlns:local="using:Pango.Desktop.Uwp.Models"
    xmlns:views="using:Pango.Desktop.Uwp.Views"
    xmlns:abstract="using:Pango.Desktop.Uwp.Views.Abstract"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance Type=viewmodels:PasswordsViewModel}"
    d:DesignHeight="600"
    d:DesignWidth="800">
    <Page.Resources>

        <DataTemplate x:Key="UniversalExplorerItemTemplate" x:DataType="local:PangoExplorerItem">
            <TreeViewItem ItemsSource="{Binding Children}" 
                          IsExpanded="{Binding IsExpanded, Mode=TwoWay}"
                          Padding="0"
                          HorizontalContentAlignment="Stretch">

                <TreeViewItem.ContextFlyout>
                    <MenuFlyout>
                        <MenuFlyoutItem Text="{loc:StringResource Key=Edit}" 
                                        Icon="Edit" 
                                        Click="EditContextMenuItem_Click"/>
                        <MenuFlyoutItem Text="{loc:StringResource Key=Delete}" 
                                        Icon="Delete" 
                                        Click="DeleteContextMenuItem_Click"/>
                        <MenuFlyoutSeparator/>
                        <MenuFlyoutItem Text="{loc:StringResource Key=AddPassword}" 
                                        Icon="Add" Click="AddPassword_CatalogContextMenuItem_Click" 
                                        Visibility="{Binding IsFolder, Converter={StaticResource BoolToVisibilityConverter}}"/>
                        <MenuFlyoutItem Text="{loc:StringResource Key=AddNewCatalog}" 
                                        Icon="NewFolder" Click="AddCatalog_CatalogContextMenuItem_Click" 
                                        Visibility="{Binding IsFolder, Converter={StaticResource BoolToVisibilityConverter}}"/>
                        <MenuFlyoutItem Text="{loc:StringResource Key=CopyPassword}" 
                                        Icon="Copy" Click="CopyPassword_PasswordContextMenuItem_Click" 
                                        Visibility="{Binding IsFolder, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=reverse}"/>
                        <MenuFlyoutItem Text="{loc:StringResource Key=SeePassword}" 
                                        Click="SeeContextMenuItem_Click" 
                                        Visibility="{Binding IsFolder, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=reverse}">
                            <MenuFlyoutItem.Icon>
                                <FontIcon Glyph="&#xE7B3;"/>
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                    </MenuFlyout>
                </TreeViewItem.ContextFlyout>

                <Grid HorizontalAlignment="Stretch"
                      Background="Transparent"
                      ToolTipService.ToolTip="{Binding ExpirationTooltip}"
                      AutomationProperties.Name="{Binding Name}"
                      DoubleTapped="Password_DoubleTappedAsync">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="28"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <FontIcon Grid.Column="0" 
                              Glyph="&#xE814;" 
                              FontSize="14" 
                              Foreground="Red" 
                              Visibility="{Binding ExpirationStatus, Converter={StaticResource ExpirationStatusToVisibilityConverter}, ConverterParameter=Expired}" />
                    <FontIcon Grid.Column="0" 
                              Glyph="&#xE814;" 
                              FontSize="14" 
                              Foreground="#FFC107" 
                              Visibility="{Binding ExpirationStatus, Converter={StaticResource ExpirationStatusToVisibilityConverter}, ConverterParameter=ExpiringSoon}" />

                    <Image Grid.Column="1" 
                           Source="{StaticResource SharedFolderImage}" 
                           Width="20" 
                           Height="20" 
                           VerticalAlignment="Center" 
                           Stretch="Uniform" 
                           Margin="5,2,8,2"
                           Visibility="{Binding IsFolder, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    
                    <FontIcon Grid.Column="1" 
                              Glyph="&#xE8D7;" 
                              FontSize="{StaticResource FontIconSize}" 
                              VerticalAlignment="Center" 
                              Margin="5,2,8,2"
                              Visibility="{Binding IsFolder, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=reverse}"/>

                    <TextBlock Grid.Column="2" 
                               Text="{Binding Name}" 
                               VerticalAlignment="Center" />
                    <StackPanel Grid.Column="3" 
                                Orientation="Horizontal" 
                                HorizontalAlignment="Right"
                                Margin="0,0,24,0"
                                Visibility="{Binding IsFolder, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=reverse}">

                        <Button Margin="2" 
                                Background="Transparent" 
                                BorderThickness="0" 
                                ToolTipService.ToolTip="{loc:StringResource Key=ToggleStar}" 
                                Click="ToggleStarButton_Click">
                            <FontIcon Glyph="{Binding IsStar, Converter={StaticResource BoolToStarGlyphConverter}}" 
                                      FontSize="{StaticResource FontIconSize}" 
                                      Foreground="{Binding IsStar, Converter={StaticResource BoolToStarColorConverter}}"/>
                        </Button>

                        <Button Margin="2" 
                                Background="Transparent" 
                                BorderThickness="0" 
                                ToolTipService.ToolTip="{loc:StringResource Key=CopyPassword}" 
                                Click="CopyPasswordButton_Click">
                            <FontIcon Glyph="&#xE8C8;" 
                                      FontSize="{StaticResource FontIconSize}"/>
                        </Button>

                        <Button Margin="2" 
                                Background="Transparent" 
                                BorderThickness="0" 
                                ToolTipService.ToolTip="{loc:StringResource Key=SeePassword}" 
                                Click="SeePasswordButton_Click">
                            <FontIcon Glyph="&#xE7B3;"
                                      FontSize="{StaticResource FontIconSize}"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </TreeViewItem>
        </DataTemplate>

    </Page.Resources>

    <Grid x:Name="RootGrid">
        <Pivot x:Name="PasswordsIndex_Pivot" IsLocked="True">
            <Pivot.HeaderTemplate>
                <DataTemplate>
                    <StackPanel Width="0" 
                                Height="0">
                        <TextBlock Text="{Binding}" />
                    </StackPanel>
                </DataTemplate>
            </Pivot.HeaderTemplate>

            <PivotItem Margin="0, -65, 0, 0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Margin="20" 
                               Text="{loc:StringResource Key=MyPasswords}" 
                               Style="{StaticResource HeaderTextBlockStyle}"/>

                    <Grid Grid.Row="1">

                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Grid Margin="15, 0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <controls:SearchTextBox MinWidth="300"
                                                    VerticalAlignment="Center"
                                                    HorizontalAlignment="Stretch"
                                                    SearchCommand="{Binding Path=SearchCommand}"
                                                    Text="{Binding Path=SearchText, Mode=TwoWay}"
                                                    PlaceholderText="{loc:StringResource Key=SearchPasswords}"
                                                    Visibility="{Binding Path=ShowInitialScreen, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=reverse}"/>

                            <CommandBar IsOpen="False"
                                        Grid.Column="1" 
                                        Background="Transparent"
                                        DefaultLabelPosition="Right"
                                        Visibility="{Binding Path=ShowInitialScreen, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=reverse}">

                                <AppBarButton Icon="Add" 
                                              LabelPosition="Collapsed" 
                                              Command="{Binding Path=CreatePasswordCommand}"
                                              ToolTipService.ToolTip="{loc:StringResource Key=AddNewPassword}"/>
                                <AppBarButton Icon="NewFolder" 
                                              LabelPosition="Collapsed" 
                                              Command="{Binding Path=CreateCatalogCommand}" 
                                              ToolTipService.ToolTip="{loc:StringResource Key=AddNewCatalog}"/>
                                <AppBarButton Icon="Edit" 
                                              LabelPosition="Collapsed" 
                                              Command="{Binding Path=EditPasswordCommand}" 
                                              CommandParameter="{Binding SelectedItem}" 
                                              ToolTipService.ToolTip="{loc:StringResource Key=Edit}"/>
                                <AppBarButton Icon="Delete" 
                                              LabelPosition="Collapsed" 
                                              Command="{Binding Path=DeleteCommand}" 
                                              CommandParameter="{Binding SelectedItem}"
                                              ToolTipService.ToolTip="{loc:StringResource Key=Delete}"/>

                                <AppBarSeparator />

                                <AppBarToggleButton Icon="OutlineStar"
                                                    LabelPosition="Collapsed"
                                                    IsChecked="{Binding ShowOnlyStarred, Mode=TwoWay}"
                                                    ToolTipService.ToolTip="{loc:StringResource Key=ShowFavorites}"/>

                                <AppBarButton Icon="Refresh" 
                                              LabelPosition="Collapsed"
                                              Command="{Binding Path=UpdateListCommand}"
                                              ToolTipService.ToolTip="{loc:StringResource Key=UpdatePasswordList}"/>
                            </CommandBar>
                        </Grid>

                        <Grid Grid.Row="1"
                              Margin="10"
                              Visibility="{Binding Path=ShowInitialScreen, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=reverse, FallbackValue=Collapsed}">

                            <TreeView x:Name="PasswordsTreeView"
                                      DragItemsCompleted="PasswordsTreeView_DragItemsCompletedAsync"
                                      ItemsSource="{Binding Path=Passwords}"
                                      ItemTemplate="{StaticResource UniversalExplorerItemTemplate}"
                                      Visibility="{Binding Path=HasPasswords, Converter={StaticResource BoolToVisibilityConverter}, FallbackValue=Visible}">
                            </TreeView>

                            <TextBlock Text="{loc:StringResource Key=NoPasswordsFound}" 
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Visibility="{Binding Path=HasPasswords, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=reverse, FallbackValue=Visible}"/>
                        </Grid>
                    </Grid>

                    <Grid Grid.Row="1" 
                          Visibility="{Binding Path=ShowInitialScreen, Converter={StaticResource BoolToVisibilityConverter}, FallbackValue=Collapsed}">
                        <StackPanel Orientation="Vertical" 
                                    VerticalAlignment="Center" 
                                    HorizontalAlignment="Center">
                            <TextBlock Text="{loc:StringResource Key=NoPasswordsYet}" 
                                       HorizontalAlignment="Center"/>
                            <StackPanel Orientation="Horizontal">
                                <Button Margin="10"
                                        Content="{loc:StringResource Key=AddPassword}"
                                        HorizontalAlignment="Center"
                                        Style="{StaticResource AccentButtonStyle}"
                                        Command="{Binding Path=CreatePasswordCommand}"/>
                                <TextBlock Margin="5" 
                                           VerticalAlignment="Center" 
                                           Text="{loc:StringResource Key=Or}"/>
                                <Button Margin="10"
                                        Content="{loc:StringResource Key=AddNewCatalog}" 
                                        HorizontalAlignment="Center" 
                                        Style="{StaticResource AccentButtonStyle}"
                                        Command="{Binding Path=CreateCatalogCommand}"/>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </Grid>
            </PivotItem>

            <PivotItem>
                <views:EditPasswordView />
            </PivotItem>
        </Pivot>
    </Grid>
</abstract:PageBase>
    